# 项目实施里程碑

本文档用于跟踪 RGPerp 的实施进度。任务口径与 `spec/TECH_ARCHITECTURE.md`、`spec/DATABASE_SCHEMA.md`、`spec/API_SPEC.md` 保持一致，仅保留面向交付的实施事项，不记录需求讨论过程。

## 使用方式

- 每个里程碑完成后，将标题前的复选框打勾
- 若里程碑内子项尚未全部完成，不勾选该里程碑
- 如实施中发生架构调整，应先更新 `spec/` 文档，再更新本任务表

---

- [x] 里程碑 1：基础工程与运行环境就绪
  - 建立 `frontend/`、`backend/`、`contracts/` 目录与基础脚手架
  - 完成 `Docker Compose`，启动 `MySQL`、`Redis`、`RabbitMQ`
  - 完成后端配置加载、日志、中间件、数据库连接与健康检查
  - 完成前端 `React + Vite + TypeScript + Ant Design` 基础工程
  - 完成 Solidity / Foundry 工程初始化、本地部署脚本与链下服务骨架
  - 完成标准：本地可启动基础依赖，前后端、合约工程与三类后台进程均可独立运行
- [x] 里程碑 2：账户体系、Vault 与链上记账闭环
  - 实现钱包登录、nonce 签名验证、JWT 会话机制
  - 完成 `MockUSDC` 与 `Vault` 合约，支持 `deposit` / `withdraw`
  - 实现后端签名授权提现模型
  - 实现 `Indexer`，完成 `Deposit` / `Withdraw` 事件监听、幂等处理与账本入账
  - 完成账户余额、资金流水、充值信息、提现申请接口
  - 完成标准：用户可完成登录、充值、提现，链上事件可稳定同步到账户余额与账本
- [ ] 里程碑 3：行情、交易引擎与仓位系统
  - [x] 接入价格服务（`mock`），并预留 `Hyperliquid` 行情源配置入口
  - [x] 完成 symbol 配置与 ticker 查询接口（`GET /markets`、`GET /markets/:symbol/ticker`）
  - [x] 完成 K 线查询接口（`GET /markets/:symbol/klines`）
  - [ ] 实现 CFD 模式市价单执行
  - [ ] 实现仓位开仓、加仓、减仓、平仓、反手逻辑
  - [ ] 实现手续费、已实现盈亏、未实现盈亏、清算价格计算
  - [ ] 完成前端交易页、资产页、仓位页联调（已完成行情 + K线联调）
  - 完成标准：用户可以在前端完成单一 symbol 的完整交易与仓位查看闭环
- [ ] 里程碑 4：风险控制、清算与对冲系统
  - 实现初始保证金、维持保证金、账户权益和风险率计算
  - 实现交易前风控校验与风险状态冻结
  - 实现 `Hedging Bot`，按净敞口对冲至 `Hyperliquid Testnet`
  - 实现对冲状态跟踪、失败重试、偏差监控与风险告警
  - 实现 `Liquidator`，支持风险扫描、强平执行、反向对冲
  - 完成标准：系统可在风险条件触发时执行强平，并保持平台净敞口在可控阈值内
- [ ] 里程碑 5：联调、质量保障与交付物完善
  - 完成核心接口集成测试、关键账务测试、合约测试和异常流程回归
  - 完成前端错误态、加载态、交易结果态与风控提示
  - 完成管理侧风险观察接口与运行状态检查
  - 更新 `README.md`、`Architecture.md`、`AI_REPORT.md` 与 `spec/` 文档
  - 整理示例环境变量、启动命令、演示路径与已知限制
  - 完成标准：仓库达到可交付状态，本地可完整演示登录、充值、交易、对冲、平仓、提现、清算主流程
