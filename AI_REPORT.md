# AI Usage Report

## 1. 使用的 AI 工具及模型

本项目主要使用 `Cursor` 的 Agent 能力完成方案设计、代码实现、问题定位、测试补充与文档整理。  
AI 的角色并不是“自动生成整套系统后直接提交”，而是作为高密度协作工具，用于提升方案探索速度、减少样板代码编写时间，并在反复审查中辅助发现逻辑漏洞。

本项目实际采用的 AI 工具如下：

| 工具 | 用途 | 使用阶段 | 模型 |
| --- | --- | --- | --- |
| Cursor Agent | 架构设计、后端实现、前端页面开发、测试补充、代码审查、文档撰写 | 全程 | GPT-5.4 |
| Cursor 内置代码编辑与检索能力 | 大规模代码搜索、跨文件重构、定位风控与清算问题 | 开发与调试阶段 | Cursor 内置能力 |

说明：

- 本项目 AI 使用的重点不在“单次生成大段代码”，而在多轮对话式迭代。
- 对于资金、风险、清算、对冲这类高风险模块，AI 先给出候选实现，再由人工反复审查与回归测试。
- 对于文档、Landing Page、接口草案、测试用例和运行脚本，AI 的加速效果最明显。

## 2. AI 参与的主要阶段

### 2.1 方案探索阶段

AI 主要用于：

- 比较 `CFD 模式` 与 `订单簿撮合模式` 的实现复杂度、交付风险与展示效果
- 评估 `Hyperliquid 行情`、`第三方 API` 与 `mock price` 在开发期的适用性
- 比较 `净敞口统一对冲` 与 `逐笔订单对冲` 两种策略
- 设计链上托管、链下交易、链下风控、链下清算的整体模块边界

这个阶段 AI 主要提供的是结构化备选方案，而不是最终答案。

### 2.2 文档与架构阶段

AI 主要用于：

- 输出 README、Architecture Document、API 规范、数据库结构草案
- 把讨论过程沉淀为专业、对外风格统一的交付文档
- 把零散实现整理成可执行 runbook，例如清算测试流程文档

### 2.3 后端实现阶段

AI 主要用于：

- 快速生成 Gin/GORM 风格的 handler、service、model 样板
- 协助实现订单、仓位、账本、清算、对冲任务等核心模块
- 生成和补充服务层测试
- 发现并修复下单、保证金、清算、对冲方向等关键逻辑问题

### 2.4 前端实现阶段

AI 主要用于：

- 搭建 React + Ant Design 基础页面结构
- 调整路由结构，例如首页与 `/app` 的拆分
- 迭代 Landing Page 的视觉表达
- 集成 React Bits 风格组件，补类型声明与样式细节

### 2.5 调试与审查阶段

AI 主要用于：

- 对关键链路做二次审查，而不是只做实现
- 定位保证金计算、仓位杠杆覆盖、最大仓位校验、对冲方向、清算逻辑等问题
- 生成回归测试，验证修复是否真实生效

## 3. 关键 Prompts 示例

以下 prompts 是本项目中最有价值的一类问题，代表 AI 主要参与的设计和实现方向。

1. 请基于链上托管、链下交易、外部对冲的约束，设计一个最小可运行的永续合约交易所架构，并明确 Frontend、Backend、Indexer、Vault、Hedger、Liquidator 的职责边界。

2. 请比较 CFD 模式与订单簿撮合模式在当前交付目标下的优缺点，重点分析实现稳定性、复杂度、可解释性和展示效果，并给出推荐方案。

3. 请设计保证金、PnL、维持保证金、风险率、reduce-only、liquidation、insurance fund 之间的关系，并给出下单到爆仓的完整状态流转。

4. 请重新审查下单逻辑，重点检查开仓、加仓、减仓、反手、锁定保证金、释放保证金、账本写入是否一致，找出可能导致资金口径不一致的问题。

5. 请重新分析系统的对冲逻辑。假设内部净敞口为用户总共开多 `100 BTC`，外部正确的对冲方向应该是什么？请解释平台 PnL 与外部对冲 PnL 如何相互抵消。

6. 请审查清算逻辑是否能够做到自动清算、强制平仓，并验证下单、风险升级、爆仓、账本、对冲任务生成这条完整链路是否闭环。

7. 请基于现有项目生成一份可重复执行的清算测试 runbook，要求包含启动步骤、登录、充值、开仓、打低价格、触发清算、数据库与 API 验证方法。

8. 请将 AI 使用报告按照交付要求扩写为正式文档，详细说明使用工具、关键 prompts、代码比例估计、AI 帮助解决的问题、AI 的边界以及人工校正方法。

## 4. AI 生成代码比例估计

以下比例为经验估计，目的是说明 AI 的参与方式，而不是追求更高自动化比例。

- 架构与交付文档：`70% - 85%`
  - AI 非常适合先生成结构化文档初稿，再由人工统一术语、补充关键决策与删除不准确表述。

- 前端页面与样板 UI：`55% - 75%`
  - 包括交易页骨架、Landing Page、路由重构、样式调优、第三方视觉组件集成。

- 后端基础样板代码：`45% - 65%`
  - 包括 handler、service、model、基础测试、运行脚本等。

- 资金、风险、清算、对冲核心逻辑：`25% - 45%`
  - AI 常能给出初稿或局部修复建议，但关键公式、方向判断、边界条件必须由人工主导校正。

- 最终测试、审查、问题归因：`20% - 35%`
  - AI 可以辅助定位问题、生成候选测试，但“是否真的正确”依赖人工验证。

总体判断：

- AI 在“方案探索、代码加速、文档整理、测试补充”上贡献明显。
- 在“资金安全、爆仓逻辑、对冲方向、系统一致性”上，人工判断仍然是决定性因素。

## 5. AI 帮助解决的问题

### 5.1 快速完成系统级需求拆解

项目需求本身覆盖：

- 链上资金托管
- 链下交易执行
- 保证金与风控
- 清算与强平
- 净敞口对冲
- 前端交易与运营展示

AI 在前期最重要的价值，是把这些模块从“要求列表”整理成“可实现的系统结构”，减少方案发散成本。

### 5.2 帮助做技术路线比较

例如以下关键设计决策都通过 AI 进行了方案比较和优缺点分析：

- CFD 模式 vs 订单簿撮合
- 单 symbol 起步 vs 多 symbol 设计
- 净敞口统一对冲 vs 逐笔订单对冲
- mock / Binance / Hyperliquid / oracle 价格源的阶段性使用策略
- 后端签名提现 vs 更简单但更弱的提现模型

这些讨论帮助项目较早确定了稳定路线，避免在高复杂度方案上浪费实现时间。

### 5.3 提升文档交付效率

AI 对文档效率的帮助非常明显，尤其包括：

- README
- Architecture Document
- API 规范
- 数据库结构文档
- 清算测试 Runbook
- 本文档 AI Usage Report

文档最终仍由人工统一风格与校正内容，但 AI 显著降低了从 0 到 1 的写作成本。

### 5.4 加速后端实现与测试补充

AI 在后端开发中帮助完成了：

- 订单、仓位、账本、风险、清算、对冲等模块的初版实现
- `gorm` 事务、行锁、乐观锁、账本写入等代码的整理
- 单元测试与回归测试补充
- 清算链路、保证金校验、对冲 drift 计算等逻辑复查

更重要的是，AI 不只是“生成代码”，也在后续多轮审查中帮助识别出：

- 对冲方向错误
- 自定义保证金校验缺失
- 风控初始保证金口径与真实锁仓金额不一致
- 最大仓位只校验单笔、不校验聚合仓位
- 小额加仓覆盖整仓杠杆

### 5.5 辅助前端效果快速迭代

Landing Page 是一个典型例子。AI 在这个过程中承担了：

- 初版布局生成
- 路由结构改造
- React Bits 风格组件接入
- `MagicRings`、`BlobCursor`、品牌图标与 CTA 的参数迭代

前端最终效果不是 AI 一次性生成的，而是通过多轮反馈逐步逼近目标。

## 6. AI 无法完全解决的问题

### 6.1 无法替代真实交易系统经验

AI 可以给出合理的清算、风险、保险基金、对冲设计候选方案，但这些方案是否足够安全，仍然高度依赖人工经验判断。  
尤其是以下问题，AI 不能被直接信任为最终结论：

- 爆仓阈值是否合理
- 维持保证金比例如何设置
- 清算分档比例是否合适
- 对冲失败后的熔断规则是否足够保守

### 6.2 无法自动保证资金相关代码正确

资金和账本代码最容易出现“局部看起来合理，但全链路口径不一致”的问题。  
例如本项目中实际发现过：

- 下单口径与风控口径不一致
- 整仓杠杆被后续小单覆盖
- 聚合仓位上限没有被正确限制

这些问题即使 AI 能帮忙生成代码，也不能假设天然正确。

### 6.3 无法替代真实联调与环境验证

AI 无法代替以下工作：

- 合约、Indexer、Backend、Liquidator、Hedger 之间的真实联调
- 本地链 / 数据库 / Redis / RabbitMQ 组合环境验证
- 价格源中断、价格陈旧、外部 API 失败等运行时故障测试

因此项目后期必须通过运行脚本、服务测试和手工链路测试反复校验。

### 6.4 无法自动覆盖所有极端场景

AI 通常更擅长主路径，而不是极端边界条件。  
例如以下问题仍需要人工继续完善：

- stale price 下的清算保护
- 保险基金不足时的系统亏空记账
- 部分清算后的多仓位再评估
- 外部对冲部分成交、失败、重试、漂移累积
- 链重组、重复事件、消息重复消费

### 6.5 无法替代最终产品判断

在前端视觉上，AI 能快速生成多个方向，但是否达到“成品官网感”、是否协调、是否符合目标审美，最终仍然是人工判断。

## 7. 人工校正与验证方式

为了避免 AI 输出直接进入最终交付而带来风险，本项目采用了以下人工校正方式。

### 7.1 人工审查所有资金与风险相关代码

重点人工复查的模块包括：

- `order`
- `risk`
- `liquidator`
- `hedge`
- `indexer`
- `withdrawal`
- `ledger`

特别关注：

- 保证金锁定 / 释放
- 已实现与未实现 PnL
- 风险状态切换
- 清算执行价
- 保险基金扣减
- 对冲方向与 drift 计算

### 7.2 用测试而不是口头解释验证逻辑

项目中多次采用“先审查，再补回归测试”的方式验证逻辑。  
例如在发现保证金与仓位风险问题后，补充了服务层测试来确认：

- 自定义保证金下限
- 聚合仓位上限
- 有效杠杆重算
- 风控 `TotalInitial` 与真实锁仓金额一致

### 7.3 人工执行完整链路联调

人工联调覆盖了：

- challenge / login
- 本地链 mint / approve / deposit
- indexer 入账
- 下单与仓位更新
- 提现签名与链上提现
- 手动插入 `price_ticks` 触发清算
- 清算记录、交易记录、对冲任务验证

### 7.4 对 AI 输出进行二次审查而不是直接采纳

本项目中，AI 经常先给出一个实现方向，但后续会再要求 AI 或人工对已实现代码做反向审查。  
这类“先实现，再怀疑，再测试”的流程，在风控系统里非常重要。

## 8. 本项目中 AI 的使用原则

本项目实际遵循了以下使用原则：

1. AI 负责加速，不负责兜底。
2. 资金、风险、清算、对冲类逻辑必须经过人工复查。
3. 任何重要修复都尽量补回归测试。
4. 文档可以大量借助 AI 起草，但结论必须与真实实现一致。
5. 发现 AI 生成结果与业务直觉冲突时，优先相信审查、公式和测试，而不是相信第一版生成结果。

## 9. 最终说明

本项目中，AI 主要用于加速方案探索、生成样板代码、补充测试和整理交付文档。  
对于前端页面、接口文档、架构文档和基础服务代码，AI 显著提升了开发效率。  
对于保证金、清算、对冲、账本一致性等高风险模块，AI 只提供候选实现和辅助审查，最终正确性依赖人工设计判断与反复测试。  
最终交付质量并不来自“AI 自动生成”，而来自“AI 加速 + 人工校正 + 回归测试 + 联调验证”的组合流程。

